// Single source of truth for the app version - bump on EVERY change (YYYY.MM.DD.NN).
// The service worker registration carries it as ?v= so a deploy busts the cache.
export const APP_VERSION = 'alpha.01.03.15';
